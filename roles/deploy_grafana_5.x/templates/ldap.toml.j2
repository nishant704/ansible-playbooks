# Set to true to log user information returned from LDAP
verbose_logging = true

[[servers]]
# Ldap server host (specify multiple hosts space separated)
host = "172.16.8.5"
# Default port is 389 or 636 if use_ssl = true
port = 389
# Set to true if ldap server supports TLS
use_ssl = false
# set to true if you want to skip ssl cert validation
ssl_skip_verify = false
# set to the path to your root CA certificate or leave unset to use system defaults
# root_ca_cert = /path/to/certificate.crt

# Search user bind dn
bind_dn = "cn=admin,dc=paytmlabs,dc=com"
# Search user bind password
bind_password = 'l1st4llth3us3rs!!'

# User search filter, for example "(cn=%s)" or "(sAMAccountName=%s)" or "(uid=%s)"
#search_filter = "(cn=%s)"
search_filter = "(uid=%s)"

# An array of base dns to search through
search_base_dns = ["dc=paytmlabs,dc=com"]


# In POSIX LDAP schemas, without memberOf attribute a secondary query must be made for groups.
# This is done by enabling group_search_filter below. You must also set member_of= "cn"
# in [servers.attributes] below.

## Group search filter, to retrieve the groups of which the user is a member (only set if memberOf attribute is not available)
group_search_filter = "(&(objectClass=posixGroup)(memberUid=%s))"
## An array of the base DNs to search through for groups. Typically uses ou=groups
group_search_base_dns = ["dc=paytmlabs,dc=com"]

# Specify names of the ldap attributes your ldap uses
[servers.attributes]
name = "givenName"
surname = "sn"
username = "cn"
member_of = "cn"
mail =  "email"


# Map ldap groups to grafana org roles
[[servers.group_mappings]]
group_dn = "grafana-admin"
org_role = "Admin"
org_id = 1

[[servers.group_mappings]]
group_dn = "grafana-admin"
org_role = "Admin"
org_id = 2

[[servers.group_mappings]]
group_dn = "grafana-admin"
org_role = "Admin"
org_id = 5

[[servers.group_mappings]]
group_dn = "mayassar-level2"
org_role = "Admin"
org_id = 1

[[servers.group_mappings]]
group_dn = "mayassar-level1"
org_role = "Admin"
org_id = 2

[[servers.group_mappings]]
group_dn = "grafana-managers"
org_role = "Admin"
org_id = 2

[[servers.group_mappings]]
# If you want to match all (or no ldap groups) then you can use wildcard
group_dn = "grafana-users"
org_role = "Editor"
org_id = 2

[[servers.group_mappings]]
group_dn = "grafana-users"
org_role = "Viewer"
org_id = 1

[[servers.group_mappings]]
group_dn = "mayassar-level2"
org_role = "Admin"
org_id = 5

[[servers.group_mappings]]
group_dn = "board-dashboards"
org_role = "Viewer"
org_id = 5

[[servers.group_mappings]]
group_dn = "mayassar-level2"
org_role = "Admin"
org_id = 6

[[servers.group_mappings]]
group_dn = "boxoffice-screens"
org_role = "Viewer"
org_id = 6

[[servers.group_mappings]]
group_dn = "mayassar-level2"
org_role = "Admin"
org_id = 8

[[servers.group_mappings]]
group_dn = "growth-team"
org_role = "Viewer"
org_id = 8

[[servers.group_mappings]]
group_dn = "mayassar-level2"
org_role = "Admin"
org_id = 10

[[servers.group_mappings]]
group_dn = "travel-team"
org_role = "Viewer"
org_id = 10

[[servers.group_mappings]]
group_dn = "bank-users"
org_role = "Viewer"
org_id = 12

[[servers.group_mappings]]
group_dn = "mayassar-level2"
org_role = "Admin"
org_id = 12
